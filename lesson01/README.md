# Блок 1. Инфраструктура и Docker

## Первый день в SOC

> Понедельник, 9:00. Алексей Морозов нервно поправляет бейдж "FinanceFlow" — первый рабочий день в жизни. Офис на 15-м этаже бизнес-центра, панорамные окна, запах свежего кофе. HR-менеджер провела по этажу и передала в руки Марины Волковой — руководителя команды безопасности.
>
> "Добро пожаловать в SOC", — Марина указала на ряд мониторов с графиками и логами. — "Не пугайся, большая часть алертов — ложные срабатывания. Твоя первая задача — развернуть локальный стенд. У нас всё на Docker, так что начнёшь с основ."
>
> К 11:30 Алексей разобрался с docker-compose и почти закончил настройку, когда Slack-канал #security-alerts взорвался уведомлениями. На экране Марины замигал красный индикатор. "Unusual authentication pattern on payment-gateway", — прочитала она вслух, нахмурившись.
>
> "Что это значит?" — спросил Алексей. Марина уже печатала что-то в терминале. "Пока не знаю. Много неудачных попыток входа с внешних IP. Может быть ничего, а может быть... Денис!" — она окликнула проходившего мимо DevOps-инженера. — "Глянь логи payment-gateway за последний час."
>
> Денис Крылов закатил глаза, но подошёл. "Опять ваши алерты? Ладно, сейчас посмотрим." Марина повернулась к Алексею: "Пока мы разбираемся — доделай стенд. Вот генератор тестовых данных, он симулирует нашу продакшн-нагрузку. Потренируйся, возможно скоро понадобятся твои навыки."

---

## Что такое Docker и какие задачи он решает?

Представьте ситуацию: вам нужно быстро развернуть базу данных для анализа логов, потом добавить систему визуализации, а через неделю всё это удалить, потому что расследование завершено. Раньше это означало бы часы на установку, настройку зависимостей и мучительную очистку системы. Docker решает эту проблему — он позволяет запускать приложения в изолированных контейнерах, которые легко создать и так же легко удалить.

Docker появился в 2013 году и быстро стал стандартом индустрии. Причина проста: он устранил проблему "у меня на машине работает". Контейнер содержит всё необходимое для работы приложения — код, библиотеки, настройки. Если контейнер работает у вас, он будет работать и у коллеги.

## Зачем Docker специалисту по информационной безопасности?

Docker стал стандартом для развёртывания программного обеспечения, в том числе инструментов ИБ. Практически все современные решения — TheHive, MISP, Wazuh, Security Onion, Elastic Stack — распространяются как Docker-образы.

Понимание контейнеризации необходимо для:

__Развёртывания инфраструктуры мониторинга.__ SIEM-системы, сборщики логов, визуализаторы — всё это проще запустить в контейнерах, чем устанавливать вручную.

__Изоляции инструментов анализа.__ При расследовании инцидента важно, чтобы инструменты не влияли друг на друга и на хостовую систему. Контейнер создаёт изолированную среду.

__Воспроизводимости окружения.__ Когда коллега или следователь хочет повторить ваш анализ, достаточно передать Dockerfile — результат будет идентичным.

__Быстрого развёртывания honeypot.__ Контейнеры позволяют за минуты поднять ловушку для злоумышленников и так же быстро её уничтожить.

Docker также активно используется в разработке. Dev Containers — это способ описать окружение разработчика в виде контейнера. Вы открываете проект, и VS Code или Cursor автоматически разворачивает контейнер со всеми нужными инструментами. Это особенно полезно, когда в команде работают люди с разными операционными системами.

## Основы Docker

### Контейнер vs виртуальная машина

Виртуальная машина эмулирует полный компьютер с собственной ОС. Контейнер использует ядро хостовой системы, изолируя только процессы и файловую систему. Это делает контейнеры легче и быстрее.

```
┌─────────────────────────────────────┐
│           Виртуализация             │
├─────────┬─────────┬─────────────────┤
│  App A  │  App B  │     App C       │
├─────────┼─────────┼─────────────────┤
│ Guest OS│ Guest OS│    Guest OS     │
├─────────┴─────────┴─────────────────┤
│            Hypervisor               │
├─────────────────────────────────────┤
│              Host OS                │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│          Контейнеризация            │
├─────────┬─────────┬─────────────────┤
│  App A  │  App B  │     App C       │
├─────────┴─────────┴─────────────────┤
│           Docker Engine             │
├─────────────────────────────────────┤
│              Host OS                │
└─────────────────────────────────────┘
```

### Образ и контейнер

__Образ (image)__ — это шаблон, содержащий файловую систему и метаданные. Образ неизменяем и состоит из слоёв (layers). Каждая инструкция в Dockerfile создаёт новый слой.

__Контейнер (container)__ — это запущенный экземпляр образа. Контейнер добавляет к образу записываемый слой, где сохраняются изменения во время работы.

### Docker Registry и Docker Hub

Где хранятся образы? В реестрах (registry). __[Docker Hub](https://hub.docker.com/)__ — это публичный реестр, где сообщество и компании публикуют свои образы. Здесь вы найдёте официальные образы PostgreSQL, Nginx, Python и тысячи других.

Компании часто разворачивают приватные реестры для внутренних образов. Популярные решения: Harbor, GitLab Container Registry, AWS ECR.

Когда вы выполняете `docker pull postgres:16`, Docker сначала ищет образ локально. Не найдя его, обращается к Docker Hub и скачивает.

### Типичный сценарий работы

Допустим, вам нужна база данных PostgreSQL для анализа логов. Без Docker пришлось бы: скачать дистрибутив, установить, настроить пользователя, создать базу. С Docker весь процесс занимает одну команду.

```bash
# Скачать образ из Docker Hub
docker pull postgres:16

# Запустить контейнер
docker run -d --name my-postgres -e POSTGRES_PASSWORD=secret postgres:16

# Посмотреть запущенные контейнеры
docker ps

# Посмотреть логи
docker logs my-postgres

# Подключиться к контейнеру
docker exec -it my-postgres bash

# Остановить и удалить
docker stop my-postgres
docker rm my-postgres
```

Обратите внимание: после `docker rm` контейнер исчезает вместе со всеми данными внутри. Если данные нужно сохранить — используйте тома.

### Тома (volumes)

Данные внутри контейнера исчезают при его удалении. Чтобы сохранить данные, используйте тома:

```bash
# Именованный том (Docker управляет расположением)
docker run -d -v pgdata:/var/lib/postgresql/data postgres:16

# Bind mount (вы указываете путь на хосте)
docker run -d -v /path/on/host:/var/lib/postgresql/data postgres:16
```

__Рекомендация:__ для баз данных используйте именованные тома. Для конфигурационных файлов — bind mount.

## Dockerfile

Что если нужного образа нет на Docker Hub? Или вы хотите добавить в образ свой код? Тогда вы создаёте собственный образ с помощью Dockerfile.

Dockerfile — это текстовый файл с инструкциями для сборки образа. Каждая инструкция создаёт новый слой. Docker кэширует слои, поэтому повторная сборка происходит быстро — пересобираются только изменившиеся слои.

### Пример: Python-приложение для анализа логов

```dockerfile
# Базовый образ
FROM python:3.11-slim

# Метаданные
LABEL maintainer="security-team"

# Рабочая директория
WORKDIR /app

# Копируем зависимости отдельно (для кэширования)
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Копируем код приложения
COPY src/ ./src/

# Команда по умолчанию
CMD ["python", "src/analyzer.py"]
```

### Основные инструкции

- `FROM` — базовый образ
- `WORKDIR` — рабочая директория внутри контейнера
- `COPY` / `ADD` — копирование файлов в образ
- `RUN` — выполнение команды при сборке
- `ENV` — переменные окружения
- `EXPOSE` — документирование портов (не открывает их!)
- `CMD` / `ENTRYPOINT` — команда при запуске контейнера

### Best practices

__Минимизируйте количество слоёв.__ Объединяйте команды RUN:

```dockerfile
# Плохо: 3 слоя
RUN apt-get update
RUN apt-get install -y curl
RUN rm -rf /var/lib/apt/lists/*

# Хорошо: 1 слой
RUN apt-get update && \
    apt-get install -y curl && \
    rm -rf /var/lib/apt/lists/*
```

__Используйте slim/alpine образы.__ `python:3.11-slim` весит ~150 МБ, `python:3.11` — ~900 МБ.

__Не храните секреты в образе.__ Пароли и ключи передавайте через переменные окружения или Docker secrets.

### Распространённые ошибки

__Копирование всего проекта перед установкой зависимостей.__ Любое изменение в коде инвалидирует кэш, и зависимости устанавливаются заново. Сначала копируйте только файл зависимостей.

__Запуск от root без необходимости.__ Создавайте непривилегированного пользователя:

```dockerfile
RUN useradd -m appuser
USER appuser
```

## Docker Compose

Один контейнер — это хорошо. Но реальные системы состоят из нескольких компонентов: база данных, веб-сервер, очередь сообщений, система визуализации. Запускать каждый контейнер отдельной командой неудобно. Docker Compose решает эту проблему — вы описываете всю инфраструктуру в одном YAML-файле.

### Пример: PostgreSQL + Grafana

Допустим, вы строите систему мониторинга: PostgreSQL хранит события безопасности, Grafana визуализирует их. Вот как это выглядит в docker-compose.yml:

```yaml
services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: analyst
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: security_logs
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432"

  grafana:
    image: grafana/grafana:latest
    environment:
      GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_PASSWORD}
    ports:
      - "3000:3000"
    volumes:
      - grafana-data:/var/lib/grafana
    depends_on:
      - postgres

volumes:
  pgdata:
  grafana-data:
```

Одна команда `docker compose up -d` — и вся инфраструктура работает.

### Ключевые элементы

- __services__ — определение контейнеров. Каждый сервис — это один контейнер.
- __environment__ — переменные окружения. Используйте `${VAR}` для подстановки из `.env` файла.
- __volumes__ — монтирование томов. Именованные тома объявляются в секции `volumes` внизу.
- __ports__ — проброс портов `"хост:контейнер"`.
- __depends_on__ — порядок запуска. Grafana запустится после postgres. Важно: это не гарантирует, что postgres готов принимать соединения, только что контейнер запущен.
- __networks__ — по умолчанию Compose создаёт общую сеть для всех сервисов. Сервисы обращаются друг к другу по имени (postgres, grafana).

### Команды Docker Compose

```bash
# Запустить все сервисы
docker compose up -d

# Посмотреть статус
docker compose ps

# Посмотреть логи
docker compose logs -f grafana

# Остановить и удалить
docker compose down

# Удалить вместе с томами
docker compose down -v
```

### Файл .env

Храните секреты в `.env` файле (добавьте его в `.gitignore`):

```
DB_PASSWORD=super_secret_password
GRAFANA_PASSWORD=admin_secret
```

## Альтернативы Docker

- __Podman__ — совместим с Docker, но не требует демона и может работать без root. Команды идентичны: `podman run`, `podman build`. Хороший выбор для систем с повышенными требованиями к безопасности.
- __containerd + nerdctl__ — containerd это runtime, который использует Docker под капотом. nerdctl — CLI-инструмент, совместимый с Docker. Используется в Kubernetes.
- __Kubernetes__ — система оркестрации контейнеров. Нужен, когда у вас десятки и сотни контейнеров, требуется автомасштабирование и высокая доступность. Для учебного курса избыточен.
- __LXC/LXD__ — контейнеризация на уровне ОС, ближе к виртуальным машинам. Контейнер ведёт себя как полноценная система с init. Используется, когда нужна полная ОС в контейнере.

## Полезные ресурсы

- [Docker Documentation](https://docs.docker.com/)
- [Docker Hub](https://hub.docker.com/) — репозиторий образов
- [Awesome Docker](https://github.com/veggiemonk/awesome-docker) — коллекция ресурсов
- [Dockerfile best practices](https://docs.docker.com/develop/develop-images/dockerfile_best-practices/)

## Подготовка данных

Перед началом работы необходимо загрузить предварительно сгенерированные данные. FinanceFlow предоставляет два варианта датасета:

- __`lite`__ (~500 MB): данные за 14 дней атаки. Рекомендуется для быстрого старта и работы на маломощных компьютерах.
- __`full`__ (~2.1 GB): полный набор данных за 81 день (60 дней baseline + 14 дней атаки + 7 дней после инцидента). Необходим для полноценного анализа и сравнения производительности.

### Настройка окружения

1. Скопируйте файл `env.example` в `.env`:
   ```bash
   cp env.example .env
   ```

2. Откройте `.env` и заполните credentials:
   - __Для S3__ (получите у преподавателя): `AWS_ACCESS_KEY_ID`, `AWS_SECRET_ACCESS_KEY`
   - __Для PostgreSQL__ (можно использовать значения по умолчанию из `env.example`): `DB_USER`, `DB_PASSWORD`, `DB_NAME`
   
   Пример `.env`:
   ```bash
   # S3 credentials (получите у преподавателя)
   AWS_ACCESS_KEY_ID=your_access_key
   AWS_SECRET_ACCESS_KEY=your_secret_key
   S3_BUCKET=security-edu-data
   S3_ENDPOINT_URL=https://storage.yandexcloud.net
   S3_REGION=ru-central1
   
   # PostgreSQL (можно использовать значения по умолчанию)
   DB_USER=analyst
   DB_PASSWORD=security123
   DB_NAME=security_logs
   ```

3. Загрузите данные с помощью скрипта `bootstrap.py`:
   ```bash
   # Для версии lite (рекомендуется для начала)
   uv run bootstrap.py --version lite
   
   # Или для полной версии
   uv run bootstrap.py --version full
   ```

Скрипт автоматически:
- Скачает Parquet-файлы из S3 в директорию `data/`
- Проверит целостность файлов по SHA256 checksums
- Покажет прогресс загрузки

После завершения загрузки данные будут доступны в директории `data/lite/` или `data/full/` в формате Parquet, организованном по типам событий и дням.

## Практические задания

В этом блоке вы научитесь работать с Docker и Docker Compose, а также получите первый опыт работы с данными в формате Parquet через DuckDB.

### Задание 1.1: Настройка окружения и загрузка данных

1. Убедитесь, что у вас установлены все зависимости:
   ```bash
   uv sync
   ```

2. Настройте `.env` файл:
   - Скопируйте `env.example` в `.env`: `cp env.example .env`
   - Заполните S3 credentials (получите у преподавателя)
   - Проверьте, что переменные PostgreSQL заданы (можно использовать значения по умолчанию)

3. Загрузите данные версии `lite`:
   ```bash
   uv run bootstrap.py --version lite
   ```

4. Проверьте, что данные загружены:
   ```bash
   ls -lh data/lite/
   ```

Вы должны увидеть директории `auth_events/`, `nginx_logs/`, `dns_queries/`, `firewall_events/` и файл `manifest.json`.

### Задание 1.2: Docker Compose инфраструктура

Создайте `docker-compose.yml` для базовой инфраструктуры.

__Сервисы:__

1. __postgres__ — база данных (понадобится в следующих блоках)

__Требования:__

- PostgreSQL:
    - База данных `security_logs`
    - Пользователь и пароль из переменных окружения
    - Именованный том для данных
    - Healthcheck для проверки готовности
    - Порт 5432 проброшен на хост

- Общее:
    - Все секреты в файле `.env`
    - Добавьте том `./data:/data` для доступа к Parquet-файлам из контейнеров (опционально, для будущих блоков)

__Чеклист выполнения:__

- [ ] `docker-compose.yml` описывает сервис postgres
- [ ] `.env` файл содержит `DB_USER`, `DB_PASSWORD`, `DB_NAME`
- [ ] `docker compose up -d` запускает PostgreSQL без ошибок
- [ ] Можно подключиться к базе: `docker exec -it <postgres_container> psql -U analyst -d security_logs`

### Задание 1.3: Dockerfile для загрузки данных

В директории `app/` находится шаблон скрипта `load_data.py` для загрузки данных из Parquet-файлов в PostgreSQL. Ваша задача — упаковать этот скрипт в Docker-образ.

__Требования:__

1. __Реализуйте функцию `load_parquet_to_postgres`__ в `load_data.py`:
   - Используйте DuckDB для чтения Parquet-файлов
   - Загружайте данные пакетами (например, по 10000 строк)
   - Используйте `execute_values` для эффективной вставки в PostgreSQL

2. __Создайте `Dockerfile`__ для упаковки скрипта:
   - Используйте `debian:bookworm-slim` как базовый образ
   - Установите `uv` для управления зависимостями
   - Скопируйте `pyproject.toml` и `uv.lock` из корня репозитория
   - Установите зависимости через `uv sync`
   - Скопируйте скрипт `load_data.py`
   - Настройте запуск скрипта через `uv run`

3. __Соберите образ и протестируйте__:
   ```bash
   # Сборка образа
   docker build -t data-loader -f lesson01/Dockerfile .
   
   # Запуск загрузки (убедитесь, что PostgreSQL запущен)
   docker run --rm \
     --network host \
     -v $(pwd)/data:/data:ro \
     -e DB_HOST=localhost \
     -e DB_USER=analyst \
     -e DB_PASSWORD=security123 \
     -e DB_NAME=security_logs \
     data-loader python lesson01/app/load_data.py --version lite --table auth_events
   ```

4. __Проверьте результат__:
   ```bash
   docker exec -it security-postgres psql -U analyst -d security_logs -c "SELECT COUNT(*) FROM auth_events;"
   ```

__Подсказки:__

- Для чтения Parquet используйте DuckDB: `read_parquet('path/to/*.parquet')`
- Для преобразования timestamp из строки используйте `CAST(timestamp AS TIMESTAMP)`
- Для JSON-поля `details` используйте `json.dumps()` перед вставкой
- Обрабатывайте данные пакетами для эффективности

__Чеклист выполнения:__

- [ ] Функция `load_parquet_to_postgres` реализована
- [ ] Dockerfile создан и находится в `lesson01/`
- [ ] Образ успешно собирается
- [ ] Скрипт загружает данные в PostgreSQL
- [ ] В таблице `auth_events` есть данные (проверьте через `SELECT COUNT(*)`)

### Задание 1.4: Первое знакомство с данными

Откройте `practice.ipynb` и выполните задания по работе с данными через DuckDB. Вы научитесь:
- Подключаться к Parquet-файлам
- Изучать структуру данных
- Выполнять базовые запросы

> __Примечание:__ В следующих блоках вы научитесь искать индикаторы атаки в этих данных. Сейчас мы просто знакомимся со структурой.

### Проверка результата

После выполнения заданий запустите тесты:

```bash
pytest tests/lesson01/
```

Тесты проверят:
- Наличие загруженных Parquet-файлов
- Корректность структуры данных
- Доступность PostgreSQL
- Наличие данных в таблицах PostgreSQL (если выполнено задание 1.3)

---

## Что дальше?

Поздравляем! Вы успешно настроили инфраструктуру и загрузили данные. В этом блоке вы:

✅ Настроили окружение для работы с данными FinanceFlow  
✅ Познакомились с Docker и Docker Compose  
✅ Создали Dockerfile для упаковки приложения  
✅ Научились работать с Parquet через DuckDB  
✅ Загрузили данные в PostgreSQL  

### Следующий шаг: Сбор логов в реальном времени

В следующем блоке вы настроите __Vector__ — мощный инструмент для сбора, обработки и маршрутизации логов. Vector будет:

- Читать логи из различных источников (файлы, системные логи, сетевые потоки)
- Парсить и обогащать данные
- Отправлять их в PostgreSQL для оперативного анализа

Это позволит вам не только анализировать исторические данные, но и отслеживать события в реальном времени — именно то, что нужно для расследования инцидента, о котором говорила Марина.

> __Интрига:__ В данных, которые вы загрузили, уже есть следы подозрительной активности. В блоках 4-5 вы научитесь находить индикаторы атаки с помощью SQL и Python. Но сначала нужно настроить сбор новых логов — этим мы займёмся в следующем блоке.